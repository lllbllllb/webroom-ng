export interface Instruction {
          id: number;
          instrNumber: string;
          instrStatus: string;
          instrCreateDate: string;
          instrrejectionReason: string;
          instrCbCount: number;
          instrStatusMessageNumber: string;
          instrStatusMessageDat;
          instrSender: string;
          instrReceiver: string;
          instrDateSent: string;
}